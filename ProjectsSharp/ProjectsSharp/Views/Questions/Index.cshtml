@model ProjectsSharp.Service.Question
@{
    ViewData["Title"] = "Quizz App";
}
<div>

    @if (Model == null)
    {
        <h2 class="end">Fin du questionnaire</h2>
        <div class="titre">
            <button onclick="resetQuestions();">Recommencer</button>
        </div>
    }
    else
    {
        <h1>Question</h1>
        <div>
            <h2 class="titre">@Model.Text</h2>
            <h3 class="titre">@Model.Niveau point</h3>
            <ul class="questions">
                @foreach (var option in Model.Options)
                {
                    <li>
                        <button class="question"
                                data-id="@Model.Id-@option.Replace(" ", "").Replace("'", "&apos;")"
                                onclick="submitOption('@Model.Id', '@option.Replace(" ", "").Replace("'", "&apos;")')">
                            @option
                        </button>
                    </li>
                }
            </ul>
        </div>
    <div id="next">
    </div>
    }
    <div class="score">
        <h3>Score: <span id="score"></span></h3>
    </div>
    
</div>


@Html.AntiForgeryToken()

<script src="js/questions.js"></script>